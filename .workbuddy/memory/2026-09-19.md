# 2026-09-19 工作日志

## 接入项目治理基线（xiyu-project-governance）

### 任务
为引擎仓 `archive-pgy-uploader` 建立「变更记录 + 交接」治理基线，使多工具/多会话接力不丢上下文。

### 基线建立前的仓库状态
- 无 `STATUS.md` / `changelog/` / `experience/`；`.gitignore` 仅 3 条规则；无 git tag、脚本内无版本常量（即无版本号体系）。
- 工作区有**上一会话遗留未提交改动**：`.workbuddy/memory/2026-08-0{6,7}.md` 追加、`skill/SKILL.md` 新增「Xcode GUI Archive Post-actions」章节（36 行）。

### 生成参数
`bootstrap.py --project-name archive-pgy-uploader --type code --gitignore-type generic --version 1.0.0`
（`--type code` + `--gitignore-type generic`：代码仓库但非 Node 生态，故覆盖 gitignore 模板为通用版）。
脚本跳过既有 `.gitignore` 与 `.workbuddy/memory/MEMORY.md`（不覆盖），其余 8 个文件全部生成。

### 版本号口径（重要）
`v1.0.0` **不是发布版本**，而是「建立基线时的现状快照」。因本项目此前无版本号体系，故明确该语义并写入 `STATUS.md` §1 与 `changelog/v1.0.0.md`。
约定：**只有影响宿主调用行为的改动才升版本**（依据 governance-design.md「影响运行行为 → 升版本」），纯文档/错别字记 CR 标「无版本变更」。

### 产出
- `STATUS.md`：据实补全 §2 能力表 C1–C10、§3 三个入口参数表、§4 输入输出契约（蒲公英 API v2 字段、退出码、成功判定）、§5 依赖与外部契约、§7 五条已知问题。
- `AGENTS.md`：补仓库导航（引擎实现文件）+ 硬性约束（共享引擎禁写密钥/真路径；改 Xcode 接线前先读 §7）。
- `changelog/`：CR-001（治理基线）、CR-002（校准修正）、CHANGELOG 索引 + v1.0.0 档案。
- `experience/`：L-001 ~ L-005 经验 + execution-log.json（2 个 run）。

### 登记的问题（`STATUS.md` §7）
- `OPEN-001` medium by-design：Xcode 不热加载外部手改的共享 `.xcscheme` → Post-actions 静默不执行（症状：不上传 + 无日志）。
- `OPEN-002` low by-design：`--notes` 仅在 CLI/手动入口生效；Post-actions 入口恒取 JSON `updateDes`。
- `OPEN-003` low **open**：`jq` 是硬依赖（缺失即 `exit 1`），但 README 与 `skill/SKILL.md` 前置条件均未登记 → 文档债。
- `OPEN-004` low **open**：README「架构」文件树未列出 `archive_upload.sh` / `link-skill.sh` / `skill/` → 文档债。
- `OPEN-005` medium by-design：子模块接入要求引擎 commit 已 push origin（本地 commit 不够）。

### 两个实操发现（已沉淀）
1. **`skill/SKILL.md` 第 13 行乱码**：`测试<U+FFFD><U+FFFD>员`（字节 `ef bf bd ef bf bd`）→ 应为 `测试人员`。
   经 `git show HEAD:skill/SKILL.md` 比对确认**自 HEAD 起即存在**（历史遗留，非本次引入），按 §8.2 ③「先校准」修掉（CR-002）。
2. **`bash -n a.sh b.sh` 是假通过**（→ L-005）：bash 只把首个参数当脚本，其余当位置参数，**后续文件根本不检查**且整体 `exit 0`。
   本会话一度据此写出错误的验证命令，实测复现后修正为逐文件循环：
   `for f in a.sh b.sh; do bash -n "$f" || exit 1; done`。

### 校验（全部通过）
- `STATUS.md` frontmatter 可被 pyyaml 解析；`known_issues` 5 条与 §7 表格逐条一致；`severity`/`status` 取值合法；3 个 `entrypoints` 指向的文件均真实存在。
- `experience/execution-log.json` 合法 JSON；CR 条目数 2、L 条目数 5；逐文件 `bash -n` 通过。
- `.gitignore`：`pgy_config.sh` / `.env` / `*.log` / `.DS_Store` 命中忽略，`.env.example` 正确例外；且 `.gitignore` 本身仍在版本控制内（本机 `~/.gitignore_global` 虽含 `.gitignore` 一行，但因已 tracked 故规则可正常入库）。
- 核实凭证未泄漏：`PGY_USER_KEY` / `PGY_API_KEY` 仅以**变量名**出现在提示文案，值不写入日志/监控页/JSON。

### 环境
为校验 YAML frontmatter，在隔离环境装了 pyyaml：`~/.workbuddy/binaries/python/envs/default/bin/python`（pyyaml 6.0.3）。
